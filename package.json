{
  "name": "business-brief-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only control room for the Business Brief processing pipeline",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "8.16.3"
  }
}
